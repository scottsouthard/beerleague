<%= form_for @team do |f| %>
  <% if @team.errors.any? %>
    <h3><%= pluralize(@team.errors.count, "error") %> prohibited this team from being saved: </h3>

    <ul>
      <% if @errors %>
        <% @errors.each do |message| %>
          <li><%= message %></li>
      <% end %>
      <% end %>
    </ul>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>
  <p>
    <%= f.label :manager_id %><br>
    <%= f.collection_select(:manager_id, User.all, :id, :full_name, {include_blank: true}) %>
  </p>
  <p>
    <%= f.label :season_id %><br>
    <%= f.collection_select(:season_id, Season.all, :id, :name, {include_blank: true}) %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
