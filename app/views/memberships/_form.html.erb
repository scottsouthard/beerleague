<%= form_for(@membership) do |f| %>
  <p>
    <%= f.label :user_id %><br>
    <%= f.collection_select(:user_id, User.all, :id, :full_name, {include_blank: true}) %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>
